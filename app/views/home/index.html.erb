<% unless user_signed_in? %>
	<div class="hero-unit">
		<h1>Welcome!</h1>
		<p>
			You've found the
			<%= link_to "Callnepal.me", "http://callnepal.me" %>
			BETA app.
		</p>
		<p>
			<%= link_to "Give it a try!", new_user_registration_path, class: "btn btn-primary btn-large" %>
		</p>
	</div>
<% end %>
